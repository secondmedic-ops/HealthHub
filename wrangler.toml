name = "healthhub"
compatibility_date = "2026-09-23"

# Static SPA build output — Vite writes here.
[assets]
directory = "./dist"
# Deep links (e.g. /dashboard, /admin) fall back to index.html so
# react-router's client-side routing keeps working after a hard refresh.
not_found_handling = "single-page-application"
